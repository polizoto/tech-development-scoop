
<div class="post-list">
  {{#each posts as |post|}}
    <div class="card">
      <div class="card-header">
      <h3 >{{post.title}}</h3>
      <span>Posted by {{post.user.username}} on {{post.created_at}}</span>
      </div>
        <div class="card-body">
          <p>{{post.post_content}}</p>
        </div>
      </div>
      {{/each}}
</div>
{{!-- <ol class="post-list">
    {{#each posts as |post|}}
    <li>
    {{> post-info post }}
    </li>
    {{/each}}
</ol> --}}